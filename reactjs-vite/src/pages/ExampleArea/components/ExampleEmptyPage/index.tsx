/**
 * This is an example of the simplest page you can have (generally for "empty state").
 * 
 * It's really similar to the `ExampleChildComponent`, as there's no `#region`/`#endregion` definitions (they aren't
 * needed).
 */
export default function ExampleEmptyPage() {
    return (
        <div>
            <p className="font-bold m-4">There's nothing to show.</p>
        </div>
    )
}